import { router } from 'expo-router';
import { useState } from 'react';
import { StyleSheet, Text } from 'react-native';
import { Field, PrimaryButton, Screen } from '@/components/stock-ui';
import { useStockLite } from '@/context/StockLiteContext';
const numberValue = (value: string) => Number(value.replace(/,/g, ''));
export default function AddProductScreen() { const { addProduct } = useStockLite(); const [name, setName] = useState(''); const [cost, setCost] = useState(''); const [price, setPrice] = useState(''); const [quantity, setQuantity] = useState(''); const [threshold, setThreshold] = useState('5'); const [error, setError] = useState(''); const [isSaving, setIsSaving] = useState(false); const save = async () => { setError(''); const costValue = numberValue(cost); const priceValue = numberValue(price); const quantityValue = numberValue(quantity); const thresholdValue = numberValue(threshold); if (!name.trim()) return setError('Product name is required.'); if (![costValue, priceValue].every((value) => Number.isFinite(value) && value > 0) || ![quantityValue, thresholdValue].every((value) => Number.isFinite(value) && value >= 0) || quantityValue % 1 !== 0 || thresholdValue % 1 !== 0) return setError('Enter positive prices and valid whole-number stock values.'); try { setIsSaving(true); await addProduct({ name: name.trim(), costPrice: costValue, sellingPrice: priceValue, quantity: quantityValue, lowStockThreshold: thresholdValue }); router.back(); } catch { setError('Could not save this product. Please try again.'); } finally { setIsSaving(false); } }; return <Screen title="Add product" subtitle="Add an item to your inventory" back><Field label="Product name" value={name} onChangeText={setName} placeholder="e.g. Rice 5kg" editable={!isSaving} /><Field label="Cost price" value={cost} onChangeText={setCost} keyboardType="numeric" placeholder="0" editable={!isSaving} /><Field label="Selling price" value={price} onChangeText={setPrice} keyboardType="numeric" placeholder="0" editable={!isSaving} /><Field label="Quantity" value={quantity} onChangeText={setQuantity} keyboardType="numeric" placeholder="0" editable={!isSaving} /><Field label="Low stock threshold" value={threshold} onChangeText={setThreshold} keyboardType="numeric" placeholder="5" editable={!isSaving} />{error ? <Text style={styles.error}>{error}</Text> : null}<PrimaryButton label={isSaving ? 'Saving...' : 'Save product'} onPress={save} disabled={isSaving} /></Screen>; }
const styles = StyleSheet.create({ error: { color: '#a13f32', marginBottom: 8 } });